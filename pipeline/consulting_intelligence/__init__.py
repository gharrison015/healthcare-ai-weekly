"""Consulting intelligence utilities.

See `dedup.py` for the fuzzy-match deduplication helper. Import
explicitly to avoid eager import side-effects:

    from consulting_intelligence.dedup import is_duplicate, load_entries
"""
